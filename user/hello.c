#include "kernel/types.h" // important along side user.h as user.h uses types in types.h (SELF NOTE)
#include "user/user.h"

int main()
{
    // print
    printf("Hello, World! \n");
    
    exit(0);
}